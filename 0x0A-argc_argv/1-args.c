#include <stdio.h>
#include "main.h"
/**
 * main - checkt the code
 * @argc: parameter that count the number of argument
 * @argv: parameter tha contain the value of argument
 * Return: Always 0
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc -1);
	return (0);
}
