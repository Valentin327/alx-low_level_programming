#include "main.h"
/**
 * _abs - A function that compute the absolute value of a number
 * @n: the number to compute the absolute value
 * Return: the absolute value of an integer
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
