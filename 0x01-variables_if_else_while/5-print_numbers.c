#include <stdio.h>

/**
 * main - print lowercase alpha a-z but remove 'q' and 'e'
 * 
 *
 * Return: Always 0 (Success)  
*/

int main(void)
{
	int i;

	for (i=0; i<=10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}

