#include "main.h"

/**
 * mul - function that multiplies two numbers
 * @a: integer to multiplie with another
 * @b: integer to multiplie with another
 * Return: the product of two numbers
*/

int mul(int a, int b)
{
	int produit;

	produit = a * b;
	return (produit);
}
